<x-layout.app>
    <x-slot name="title">Services Catalog</x-slot>
    <x-slot name="subtitle">Manage products and services</x-slot>
    <div class="flex justify-between items-center mb-6"><a href="{{ route('services.create') }}" class="inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg"><ion-icon name="add-circle-outline" class="mr-2"></ion-icon>New Service</a></div>
    <x-ui.card><div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">@forelse($services as $service)<div class="border border-slate-200 rounded-lg p-4 hover:shadow-md"><div class="flex justify-between items-start mb-3"><div class="h-10 w-10 bg-blue-100 rounded-lg flex items-center justify-center"><ion-icon name="pricetag-outline" class="h-5 w-5 text-blue-600"></ion-icon></div><x-ui.badge :status="$service->is_active ? 'active' : 'inactive'" type="status"/></div><h3 class="font-semibold text-slate-900 mb-1">{{ $service->name }}</h3><p class="text-sm text-slate-500 mb-3 h-10 overflow-hidden">{{ Str::limit($service->description, 80) }}</p><div class="flex justify-between items-center pt-3 border-t"><span class="text-lg font-bold text-blue-600">Rp {{ number_format($service->base_price, 0, ',', '.') }}</span><div class="flex space-x-2"><a href="{{ route('services.edit', $service) }}" class="text-amber-600"><ion-icon name="create-outline"></ion-icon></a><form action="{{ route('services.destroy', $service) }}" method="POST" onsubmit="return confirm('Delete?')">@csrf @method('DELETE')<button type="submit" class="text-red-600"><ion-icon name="trash-outline"></ion-icon></button></form></div></div></div>@empty<div class="col-span-full text-center py-12 text-slate-500"><ion-icon name="cube-outline" class="text-5xl mb-3 block mx-auto"></ion-icon>No services found.</div>@endforelse</div>@if($services->hasPages())<div class="mt-4">{{ $services->links() }}</div>@endif</x-ui.card>
</x-layout.app>
